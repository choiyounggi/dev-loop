# Analysis — failing fixture (open-questions-resolved)

## Requirements
| Rule | Concrete example | Open question |
|------|------------------|---------------|
| R1: emit writes a ledger | given a plan-dir, when emit A runs, then out-file has 5 gates | OPEN: does emit validate plan-dir exists first? |
